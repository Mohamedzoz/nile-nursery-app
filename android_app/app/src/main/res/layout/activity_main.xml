<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <!-- 1. FIXED TOP ACTION BAR (شريط التنقل العلوي الثابت مع زرار باك وريفريش) -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentTop="true"
        android:background="@color/white"
        android:elevation="4dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <!-- Fixed Back Button (زر الرجوع الثابت) -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_back"
            android:padding="10dp"
            android:src="@drawable/ic_back" />

        <!-- Centered Logo & Title (شعار واسم حضانة النيل) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/topLogo"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:adjustViewBounds="true"
                android:contentDescription="@string/app_name"
                android:src="@drawable/logo_nile" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="@string/app_name"
                android:textColor="@color/primary_dark"
                android:textSize="17sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Fixed Refresh Button (زر التحديث الثابت) -->
        <ImageButton
            android:id="@+id/btnRefresh"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_refresh"
            android:padding="10dp"
            android:src="@drawable/ic_refresh" />

    </LinearLayout>

    <!-- 2. LOADING PROGRESS BAR -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_below="@id/topBar"
        android:indeterminate="false"
        android:max="100"
        android:progressDrawable="@color/primary"
        android:visibility="gone" />

    <!-- 3. WEBVIEW CONTAINER -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/progressBar">

        <WebView
            android:id="@+id/webView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- 4. CUSTOM OFFLINE & MAINTENANCE OVERLAY (شاشة الصيانة والأوفلاين بشعار حضانة النيل) -->
    <RelativeLayout
        android:id="@+id/offlineContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/topBar"
        android:background="#F8FAFC"
        android:clickable="true"
        android:focusable="true"
        android:padding="24dp"
        android:visibility="gone">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:orientation="vertical">

                <!-- Nursery Logo (شعار حضانة النيل البارز) -->
                <ImageView
                    android:id="@+id/offlineLogo"
                    android:layout_width="160dp"
                    android:layout_height="160dp"
                    android:layout_marginBottom="20dp"
                    android:adjustViewBounds="true"
                    android:contentDescription="@string/app_name"
                    android:src="@drawable/logo_nile" />

                <!-- Maintenance Title (العنوان الواضح دون ذكر الدومين) -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:text="@string/offline_title"
                    android:textAlignment="center"
                    android:textColor="@color/text_dark"
                    android:textSize="21sp"
                    android:textStyle="bold" />

                <!-- Friendly Description -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="28dp"
                    android:lineSpacingExtra="4dp"
                    android:text="@string/offline_desc"
                    android:textAlignment="center"
                    android:textColor="@color/text_muted"
                    android:textSize="14sp" />

                <!-- Retry Button (زر إعادة المحاولة) -->
                <Button
                    android:id="@+id/btnRetryOffline"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:layout_marginBottom="12dp"
                    android:backgroundTint="@color/primary"
                    android:text="@string/btn_retry"
                    android:textColor="@color/white"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    app:cornerRadius="16dp" />

                <!-- WhatsApp Support Button (زر واتساب المباشر) -->
                <Button
                    android:id="@+id/btnWhatsappOffline"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:backgroundTint="@color/whatsapp_bg"
                    android:text="@string/btn_contact_whatsapp"
                    android:textColor="@color/whatsapp_green"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    app:cornerRadius="16dp"
                    app:strokeColor="@color/whatsapp_green"
                    app:strokeWidth="1dp" />

            </LinearLayout>

        </ScrollView>

    </RelativeLayout>

</RelativeLayout>
